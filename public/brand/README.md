# Brand assets (drop-in)

Place the real files here and they appear automatically — no code change:

- `logo.png`   — header/footer wordmark. Transparent background, ~200×64px+.
- `founder.jpg` — Chams Eddine Madi portrait for /about (square works best).

Until these exist, the site falls back to the icon+text logo and the "CM"
monogram (see components/BrandLogo.tsx and components/FounderPhoto.tsx).

Easiest upload: GitHub web UI → this folder → Add file → Upload files.
